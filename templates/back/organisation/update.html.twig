{% extends 'base.html.twig' %}

{% block title %}Modification de l'association {{ organisation.name }}{% endblock %}

{% block body %}
    <h1>Modification de l'association {{ organisation.name }}</h1>

    {{ form_start(form )}}
        {{ form_rest(form) }}
        <button type="submit">Envoyer</button>
    {{ form_end(form) }}
{% endblock %}
